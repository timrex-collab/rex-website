VELUX Preisrechner Hero-Header einbauen
Stufe A/B | 1 TSX-Datei + 1 WebP-Asset | keine neue Route | kein Schema-Umbau | kein robots/sitemap

Ziel:
Die Seite `/velux-preisrechner-bochum` soll oben einen professionellen Hero-Header wie die übrigen Leistungsseiten erhalten. Das bisherige statische Bild im weißen Container wird entfernt und durch einen vollbreiten Hero mit Hintergrundbild, dunklem Overlay, echter H1, Kurztext und CTA ersetzt.

Neues Headerbild:
`velux-preisrechner-bochum-header-dachfenster.webp`

Das Bild ist bereits fertig optimiert im Format 1920 × 720 px. Es enthält links eine dunkle Freifläche für HTML-Text und rechts den Bildfokus auf VELUX Dachfenster mit Rollläden. Keine eingebrannten Texte oder Logos im Bild ergänzen.

Wichtig:
- Keine Preise/Kosten neu ergänzen.
- Keine Förderfakten ändern.
- Kein Wort „zertifiziert“ ergänzen.
- Keine neue Route.
- Keine Redirects.
- `robots.txt` nicht anfassen.
- `sitemap.xml` nicht anfassen.
- Schema unverändert lassen.
- Nur die angegebene TSX-Datei ändern.
- Neues Asset nur in `client/public/images/` ablegen.
- Danach `npm run build` ausführen.

────────────────────────────────────────
DATEI 1: Asset einfügen
────────────────────────────────────────

Lege diese Datei ab:

`client/public/images/velux-preisrechner-bochum-header-dachfenster.webp`

Die Datei liegt lokal bereit und muss exakt unter diesem Namen verwendet werden.

────────────────────────────────────────
DATEI 2: `client/src/pages/VeluxPreisrechnerBochum.tsx`
────────────────────────────────────────

Suche den kompletten Block:

      {/* Hero-Bild */}
      <div className="bg-white">
        <div className="max-w-3xl mx-auto px-4 pt-8">
          <img
            src="/images/velux-dachfenster-preisrechner-bochum.webp"
            alt="VELUX Dachfenster Preisrechner – Modell, Größe und Einbau konfigurieren"
            width="1200"
            height="630"
            className="w-full rounded-xl shadow-sm"
            loading="eager"
          />
        </div>
      </div>

Ersetze ihn vollständig durch:

      {/* Hero */}
      <section className="relative text-white py-24 px-4 overflow-hidden">
        <div
          className="absolute inset-0 bg-cover bg-center scale-[1.04]"
          style={{ backgroundImage: "url('/images/velux-preisrechner-bochum-header-dachfenster.webp')" }}
          aria-hidden="true"
        />
        <div
          className="absolute inset-0"
          style={{ background: "linear-gradient(135deg, rgba(15,23,42,.74), rgba(30,58,138,.46))" }}
          aria-hidden="true"
        />

        <div className="max-w-4xl mx-auto relative z-10">
          <div className="inline-flex items-center gap-2 bg-white/10 border border-white/20 rounded-full px-4 py-1 mb-6">
            <span className="text-sm font-medium text-blue-100 uppercase tracking-wider">
              VELUX Preisrechner Bochum
            </span>
          </div>

          <h1 className="text-3xl md:text-5xl font-extrabold leading-tight mb-4 max-w-3xl">
            VELUX Dachfenster Preisrechner Bochum
          </h1>

          <p className="text-lg text-slate-200 mb-8 max-w-2xl leading-relaxed">
            Konfigurieren Sie Ihr neues VELUX Dachfenster online: Fenstertyp, Größe, Verglasung und Zubehör auswählen – mit direkter Einschätzung durch Rex Bedachungs GmbH als autorisierter VELUX-Partner in Bochum.
          </p>

          <div className="flex flex-wrap gap-3">
            <a
              href="#preisrechner"
              className="inline-flex items-center justify-center rounded-md bg-white px-6 py-3 text-sm font-semibold text-blue-900 shadow-sm hover:bg-slate-100 transition-colors"
            >
              Preisrechner starten
            </a>
            <a
              href="tel:+49234583100"
              className="inline-flex items-center justify-center rounded-md border border-white/30 bg-white/10 px-6 py-3 text-sm font-semibold text-white hover:bg-white/20 transition-colors"
            >
              +49 234 583100
            </a>
          </div>
        </div>
      </section>

────────────────────────────────────────
ANKER FÜR CTA SETZEN
────────────────────────────────────────

Suche den Kommentar und die Komponente:

      {/* Rechner-Komponente */}
      <VeluxPreisrechner />

Ersetze durch:

      {/* Rechner-Komponente */}
      <div id="preisrechner">
        <VeluxPreisrechner />
      </div>

────────────────────────────────────────
META-CHECK
────────────────────────────────────────

Belasse die bestehenden OG-/Twitter-Bilder unverändert:

`/images/velux-dachfenster-preisrechner-bochum.webp`

Nicht auf das neue Headerbild umstellen, damit Social Preview und Hero getrennt bleiben.

────────────────────────────────────────
ABSCHLUSSCHECK
────────────────────────────────────────

1. Prüfe, dass nur diese Quelldatei geändert wurde:
   `client/src/pages/VeluxPreisrechnerBochum.tsx`

2. Prüfe, dass nur dieses neue Asset ergänzt wurde:
   `client/public/images/velux-preisrechner-bochum-header-dachfenster.webp`

3. Ausführen:
   `npm run build`

4. Erwartung:
   - Build erfolgreich
   - `/velux-preisrechner-bochum` zeigt oben einen vollbreiten Hero
   - Hintergrundbild ist leicht gezoomt mit `scale-[1.04]`
   - linke dunkle Freifläche trägt die echte H1 sauber
   - rechte Dachfenster bleiben sichtbar und professionell
   - CTA „Preisrechner starten“ springt zum Rechner
   - keine Änderungen an Schema, Sitemap, robots oder Routes